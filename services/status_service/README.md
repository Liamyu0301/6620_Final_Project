# Status Service

Maintains event history for each document (upload → extraction → classification → completed) and provides a query API for the frontend timeline component.

Environment variables:
- `STATUS_TABLE`
