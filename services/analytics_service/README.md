# Analytics Service

Scheduled Lambda (EventBridge rule) that aggregates metrics such as average processing time, failure counts, and number of documents processed per category.

Environment variables:
- `DOCUMENTS_TABLE`
- `ANALYTICS_TABLE`
